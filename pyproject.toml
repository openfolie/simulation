[project]
name = "simulation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mesa>=3.5.1",
    "networkx>=3.6.1",
    "pynoise>=3.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[tool.pytest.ini_options]
addopts = [
    "--ignore=opt",
]
pythonpath = ["."]

